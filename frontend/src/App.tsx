import { useState } from "react"; import { CreatePage } from "./features/generator/CreatePage"; import { HistoryPage } from "./features/history/HistoryPage"; import { SettingsPage } from "./features/settings/SettingsPage"; import { DiagnosticsPage } from "./features/diagnostics/DiagnosticsPage"; import "./index.css";
const pages = { Create: CreatePage, History: HistoryPage, Settings: SettingsPage, Diagnostics: DiagnosticsPage };
export default function App() { const [current, setCurrent] = useState<keyof typeof pages>("Create"); const Page = pages[current]; return <div className="shell"><nav><div className="brand">AVATAR<span>KIT</span><small>LOCAL-FIRST</small></div>{Object.keys(pages).map(key => <button key={key} className={current === key ? "current" : ""} onClick={() => setCurrent(key as keyof typeof pages)}>{key}</button>)}<footer>Local only<br />No accounts · No telemetry</footer></nav><Page /></div>; }
