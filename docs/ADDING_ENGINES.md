# Adding engines

Add a new adapter implementing the engine interface, a pinned manifest, an isolated runner, status checks, model verification, and explicit attribution. Do not expose unsupported controls in the UI.
