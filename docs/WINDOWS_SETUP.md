# Windows setup

Run `scripts\windows\setup.ps1 -SkipModels` for the app environment and frontend. It does not alter global packages. Inspect `doctor.ps1` before opting into large engine/model downloads.
