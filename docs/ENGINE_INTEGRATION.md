# Engine integration

SadTalker is sourced from the official OpenTalker repository and requires a separately managed compatible environment. Chatterbox is sourced from the official Resemble AI project/package and is invoked through its managed interpreter adapter. See the machine-readable manifests in `runners/`. Model downloads must be explicit, validated, and retained outside source control.
