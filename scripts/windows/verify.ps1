$ErrorActionPreference='Stop'; $root=(Resolve-Path "$PSScriptRoot\..\..").Path; if (-not $env:AVATARKIT_HOME) {$env:AVATARKIT_HOME=Join-Path $root '.avatarkit'}; $python=Join-Path $env:AVATARKIT_HOME 'environments\backend\Scripts\python.exe'; & $python -m ruff check "$root\backend"; & $python -m pytest "$root\backend\tests"; Push-Location "$root\frontend"; npm run build; Pop-Location
