# Security

AvatarKit binds to localhost by default. Do not expose it publicly without an authentication and threat-model review. API paths are job-ID scoped; arbitrary filesystem browsing is not exposed.
